package com.ldangelo.corunabuswear.data

object MessagePaths {
    object IN {
        const val DEPLOY_SETTINGS = "/deploy/settings"
    }
    object OUT {
        const val DEPLOY = "/deploy"
        const val SET_SETTINGS = "/settings/set"
    }
    const val GET_SETTINGS = "/settings/get"
    const val SETTINGS = "/settings"
}